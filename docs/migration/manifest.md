# FlowDesk Migration — Manifest & Phase Reports

> Branch: `migration/rust-backend` (from `main` = `d97a997`).
> Target stack: Next.js/TS web · Rust/Axum backend · MongoDB · Flutter/Dart mobile ·
> Python IoT service · Netlify (frontend) + Render/Fly (Rust) + MongoDB Atlas (DB).

## Phase 0 — Safe setup (COMPLETE)

### What changed
- Created branch `migration/rust-backend` from `main` (`d97a997`). `abhijit`/`prithivi` untouched.
- `flowdesk-app/.gitignore`: added `next-env.d.ts` (auto-generated by Next.js; was producing
  spurious diffs). `git rm --cached flowdesk-app/next-env.d.ts`.
- Created `backend/`, `migrations/`, `docs/migration/` workspace dirs.
- **Contract freeze**: wrote `docs/migration/contract-freeze.md` — authoritative API/behavior
  contract extracted from the Next.js route handlers (covers all 58 endpoint files, exact bodies,
  auth rules, id semantics, idempotency, validation, status codes, SSE protocol, device contract,
  env vars, and corrections to stale docs).

### What preserved
- Original implementation untouched (`flowdesk-app`, `flowdesk-mobile`, `Node/` all intact).
- Database snapshot: `C:\Users\lenovo\AppData\Local\Temp\opencode\flowdesk-snapshot-pre-migration.db`
  (checkpointed WAL → 389,120 bytes, verified: **32 tables, 300 rows**, IDs like `STU-2043`,
  `STF-118`, `ADM-004` preserved). Used as migration source-of-truth; never auto-migrated.
- `Node/` remains untracked (per earlier user decision).

### Tests / checks run
- `git checkout` + branch integrity verified.
- Snapshot integrity: table list (32 tables incl. all fingerprint tables), row counts,
  user sample query all correct.

### Known issues
- None blocking. doc `api.md` confirmed stale in 8 places (documented in contract freeze).

### Next step
- Phase 1: Rust/Axum scaffold (config, error envelope, Mongo client + typed models, scrypt +
  cookie session + JWT, CORS, logging) with pilot endpoint `GET /api/auth/me`.