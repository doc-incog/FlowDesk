/**
 * Temporary shim re-exporting seed data so existing imports keep working.
 * Will be deleted in Phase 2 once every view reads from the API/database.
 * Do not add new imports of this module in app code.
 */
export * from "@/lib/seed-data/core"
